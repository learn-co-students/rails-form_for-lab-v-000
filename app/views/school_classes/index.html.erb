<h1>School Classes List</h1>

<ul class="school-classes">
  <% @school_classes.each do |s| %>
    <li class="school-classes__klass">
      <%= link_to "#{s.title} - #{s.room_number}", school_class_path(s) %>
    </li>
  <% end %>
</ul>

<%= link_to "Add Class", new_school_class_path %>
<%= link_to "Students", students_path %>
