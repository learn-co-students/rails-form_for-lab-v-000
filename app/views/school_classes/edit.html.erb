<%= form_for(@school_class) do |f| %>

  <label>Update Class Title</label>
  <%= f.text_field :title %>

  <label>Update Class Room Number</label>
  <%= f.text_field :room_number %>

  <%= f.submit 'Update School class' %>

<% end %>
