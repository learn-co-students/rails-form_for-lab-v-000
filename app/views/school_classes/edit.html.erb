<h3>Edit School Class</h3>

<%= form_for( @school_class ) do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, value: @school_class.title %>
  <%= f.label :room_number %> 
  <%= f.text_field :room_number, value: @school_class.room_number %> 
  <%= f.submit %>
<% end %>