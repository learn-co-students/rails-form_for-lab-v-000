<h1>Update School Class</h1>

<%= form_for(@schoolclass) { |f| %>
  <%= f.text_field :title, :id => 'school_class_title' %>
  <%= f.text_area :room_number, :type => "number", :id => 'school_class_room_number' %>
  <%= f.submit %>
<% } %>