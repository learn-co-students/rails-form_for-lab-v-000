<%= form_for(@school_class) do |f| %>

<%= f.label :title %>:
<%= f.text_field :title, value: @school_class.title %><br>
<%= f.label :room_number %>:
<%= f.text_field :room_number, value: @school_class.room_number %><br>

<%= f.submit "Update School class" %>
<% end %>