<%= form_for(@school_class) do |f| %>
	School Class Form
	<%= f.label :title %>
	<%= f.text_field :title %>

	<%= f.label :room_number %>
	<%= f.number_field :room_number %>
	<%= f.submit %>
<% end %>