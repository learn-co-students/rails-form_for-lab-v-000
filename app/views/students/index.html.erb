<div>
  <% @students.each do |student| %>
    <div class="">
      <%= link_to student.to_s, student_path(student) %>
    </div>
  <% end %>
</div>

<%= button_to "Create a new Student", new_student_path, :method => "get" %>
