<h1>New Student this Semester!</h1>
<h1>Student Form!</h1>
<%= form_for :student, url:students_path do |f| %>
  <%= f.label :first_name %>:
  <%= f.text_field :first_name %><br>
  <%= f.label :last_name %>:
  <%= f.text_field :last_name %><br>

  <%= f.submit "Create Student" %>
<% end %>
