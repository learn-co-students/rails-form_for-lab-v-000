<h1>Student Form</h1>

<%= form_for :student, :url => url_for(:controller => 'students', :action => 'create') do |f| %>
  <%= f.label :first_name %>
  <%= f.text_field :first_name %>
  <%= f.label :last_name %>
  <%= f.text_field :last_name %>
  <%= f.submit 'Create Student' %>
<% end %>
