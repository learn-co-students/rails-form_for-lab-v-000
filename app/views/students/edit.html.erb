<%= form_for(@student) do |f| %>
<label>First Name: </label>
<%= f.text_field :first_name, id: 'student_first_name' %>

<label>Last Name</label>
<%= f.text_field :last_name, id: 'student_last_name' %>

<%= f.submit %>

<% end %>
