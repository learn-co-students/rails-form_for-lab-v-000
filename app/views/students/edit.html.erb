<h3>Student Form</h3>

<%= form_for(@student) do |s| %>
<label>First name:</label> <%= s.text_field :first_name %><br>
<label>Last name:<label> <%= s.text_field :last_name %><br>
<%= s.submit %>
<% end %>
