<h1>Add a Student</h1>

<%= form_for @student do |s| %>
  <p>
  <%= s.label :first_name %>
  <%= s.text_field :first_name %>
  </p>

  <p>
  <%= s.label :last_name %>
  <%= s.text_field :last_name %>
  </p>

  <%= s.submit %>
<% end %>
