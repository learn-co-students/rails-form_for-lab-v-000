<h3>Edit Student Form</h3>

<%= form_for( @student ) do |f| %>
  <%= f.label :first_name %> 
  <%= f.text_field :first_name, value: @student.first_name %>
  <%= f.label :last_name %> 
  <%= f.text_field :last_name, value: @student.last_name %> 
  <%= f.submit %>
<% end %>