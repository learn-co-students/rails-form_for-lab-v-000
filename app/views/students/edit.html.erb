<%= form_for(students_path) do |f| %>
  <%= f.label :student_first_name %> <br>
  <%= f.text_field :student[first_name] %> <br>
  <%= f.label :last_name %> <br>
  <%= f.text_field :last_name %> <br>

  <%= f.submit "Update Student" %>
  <% end %>
